# Backups (Practice)

Filesystem backups are enabled for practice.

Verify backup module:

```bash
curl -s http://localhost:8080/v1/meta \
  -H "Authorization: Bearer admin-key-123"
```

Next: [Stop / Start / Reset](10-stop-start-reset.md)
